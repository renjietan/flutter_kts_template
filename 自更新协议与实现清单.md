# 自更新协议与实现清单（最终版）

## 一、概述

- CPDS（本项目 `flutter_kts_template`，Flutter）：自更新服务器端，负责安装包上传、发现、认证、版本校验、传输、校验、回执。
- CPDC（`CPDC-main`，Go）：运行在各设备上的客户端，响应 CPDS 命令、收包组装、校验、写入安装、重启回执。
- 本协议与现有 `cpd.proto` / 现有下发功能**完全独立**。

## 二、设备标识 ID

所有上行回复（除 `Scan:success`）都携带 ID：

```text
ID = [类型字节长度 uint16BE] + [类型 UTF-8] + [IP字节长度 uint16BE] + [IP UTF-8]
```

示例（类型 `MR9360`、IP `192.168.1.10`）：

```text
00 06 MR9360 00 0C 192.168.1.10
```

## 二.1 UDP 端口

- CPDC 监听 **39003**（接收 CPDS 广播命令）。
- CPDS(flutter_kts_template) 监听 **39004**（接收 CPDC 回复）。

## 三、协议总表（最终）

### 3.1 下行指令（CPDS → CPDC）

| 指令 | 字节格式 |
|---|---|
| `Scan` | `"Scan"` |
| `auth` | `"auth"` |
| `version` | `"version:"` + 目标版本号 |
| `file:` | `"file:"` + uint32BE CRC32(整ZIP) + uint32BE 包数量 + uint32BE 文件总字节 + 文件名称 |
| `packet:` | `"packet:"` + uint32BE CRC32(本次分包) + uint32BE 包号(从1) + uint32BE 分包长度 + 分包数据 |

### 3.2 上行回复（CPDC → CPDS）

| 触发 | 回复 | 字节格式 |
|---|---|---|
| 收到 `Scan` | `Scan:success` | `"Scan:success"` |
| 收到 `auth` | `authAck` | `"authAck:"` + ID |
| 收到 `version` 且版本号格式合法 | `version_ok` | `"version_ok:"` + ID + 当前版本 |
| 收到 `version` 但版本号格式非法 | `version_fail` | `"version_fail:"` + ID + 原因 |
| 收到 `file:` | `fileAck` | `"fileAck:"` + ID + uint32BE CRC32(整ZIP) + uint32BE 包数量 + uint32BE 文件总字节 + 文件名称 |
| 收到 `packet:` 校验通过 | `packet_ok` | `"packet_ok:"` + ID + uint32BE CRC32(本次分包) + uint32BE 包号 + uint32BE 分包长度 |
| 收到 `packet:` 校验失败 | `packet_fail` | `"packet_fail:"` + ID + uint32BE CRC32(本次分包) + uint32BE 包号 + uint32BE 分包长度 + 原因 |
| 整体校验通过 | `valid_ok` | `"valid_ok:"` + ID |
| 整体校验失败 | `valid_fail` | `"valid_fail:"` + ID + 原因 |
| 更新成功 | `update_ok` | `"update_ok:"` + ID + 新版本 |
| 更新失败 | `update_fail` | `"update_fail:"` + ID + 原因 |

原因取值：

- `packet_fail`：`length`（分包数据长度 ≠ 分包长度字段）、`crc`（分包 CRC32 ≠ CRC32 字段）。
- `valid_fail`：`size`（总字节数 ≠ 文件总字节）、`crc`（整 ZIP CRC32 不符）、`zip`（无法解压）。
- `version_fail`：`format`（目标版本号格式非法）。
- `update_fail`：`install_error`（安装/写盘失败）、`restart_error`（重启失败）、`version_read_error`（重启后读取版本失败）、`version_mismatch`（版本不匹配）。

## 四、设备类型与安装文件映射

- 设备类型 = 本地 `cpdc_config.<类型>.json` 文件名的 `<类型>` 占位符。
- 若启动带 `--config-path`，配置在该目录。

| 设备类型 | 安装文件 |
|---|---|
| MR9360 | CPDC-HF |
| MMR200 | CPDC-MultiBandRadio |
| PMR200 | CPDC-MultibandHandheld |
| CCU | CPDC-CCU |
| CCUAudio | CPDC-CCU-Audio |
| VehInter | CPDC-VehInte |

> `VehInter`（类型/文件夹）与 `CPDC-VehInte`（安装文件）拼写不同，按表精确匹配。

## 五、CPDS 侧（已实现）

- 上传页面/弹窗/主流程/编辑/删除、菜单路由；上传时回写二级目录下 generic `cpdc_config.json` 的 `version`（不改 `cpdc_config.<类型>.json`）。
- 协议 codec（ID、CRC32、大端、packet_ok/packet_fail）。
- 会话状态机（超时 + 3 次重试）。
- 发现、认证、版本校验、传输、校验、写入、回执编排；版本比较在 CPDS（相等→已最新，不等→需更新；version_fail→格式非法）。

## 六、CPDC 侧实现清单（函数级）

### 6.1 通用约定

- CRC32：Go `hash/crc32` 的 `crc32.ChecksumIEEE`（与 Dart `archive` `getCrc32` 一致）。
- 字节序：全大端。
- `packet:` 头 19 字节，分包载荷 ≤ 1381，整包 ≤ 1400，包号从 1，组装下标 = 包号-1。
- `file:` 的 CRC32 = 整 ZIP；`packet:` 的 CRC32 = 单个分包。
- 重试：除 `Scan` 外，发送/收集均「初始 1 次 + 重试 2 次 = 最多 3 次」，连续 3 次超时/失败即终止。
- 所有 fail 原因在线传英文码，UI 用 i18n 转中/英/阿拉伯语。

### 6.2 device.go（T14）

```go
type DeviceInfo struct {
    DeviceType string
    IP         string
    Version    string
    RunCommand []string
    ConfigPath string
}

func DetectDeviceType(configDir string) (string, error)
func ReadVersion(configPath string) (string, error)
func DetectLocalIP() (string, error)
func ParseRunCommand(args []string) ([]string, string)
```

- `DetectDeviceType`：glob `cpdc_config.*.json`，排除 `cpdc_config.json`，返回 `*`。
- `ReadVersion`：读 `cpdc_config.json` 根节点 `version`。
- `DetectLocalIP`：取一次并缓存，后续全部复用。
- `ParseRunCommand`：保留完整命令 + 提取 `--config-path`。

### 6.3 protocol.go（T15 编解码）

```go
func encodeID(deviceType, ip string) []byte
func encodeAuthAck(d DeviceInfo) []byte
func encodeVersionOk(d DeviceInfo) []byte
func encodeVersionFail(d DeviceInfo, reason string) []byte
func encodeFileAck(d DeviceInfo, crc uint32, packetCount, totalBytes uint32, fileName string) []byte
func encodePacketOk(d DeviceInfo, crc uint32, packetNumber, packetLength uint32) []byte
func encodePacketFail(d DeviceInfo, crc uint32, packetNumber, packetLength uint32, reason string) []byte
func encodeValidOk(d DeviceInfo) []byte
func encodeValidFail(d DeviceInfo, reason string) []byte
func encodeUpdateOk(d DeviceInfo, newVersion string) []byte
func encodeUpdateFail(d DeviceInfo, reason string) []byte

func parseCommand(data []byte) command
func parseVersionCommand(data []byte) (targetVersion string, err error)  // "version:" 后取版本号并校验格式
func parseFileHeader(data []byte) (crc, packetCount, totalBytes uint32, fileName string, err error)
func parsePacket(data []byte) (crc uint32, packetNumber uint32, payload []byte, err error)
```

- `parsePacket`：跳过 `"packet:"`(7)，读 CRC32(4)、包号(4)、分包长度(4)，剩余为载荷。
- 包号 0 或越界 → 丢弃。

### 6.4 assembler.go（T15 组装）

```go
type Assembler struct {
    packetCount int
    totalBytes  int
    crc32       uint32
    fileName    string
    chunks      [][]byte // 下标 = 包号-1
}

func NewAssembler() *Assembler
func (a *Assembler) Reset()
func (a *Assembler) SetHeader(packetCount, totalBytes int, crc uint32, fileName string)
func (a *Assembler) Put(packetNumber int, payload []byte)  // 覆盖写入
func (a *Assembler) Complete() bool
func (a *Assembler) Bytes() []byte
func (a *Assembler) Validate() error  // 字节数 + CRC32
```

### 6.5 session.go（T15 会话）

```go
type Session struct {
    conn      *net.UDPConn
    info      DeviceInfo
    assembler *Assembler
    ctx       context.Context
    cancel    context.CancelFunc
}

func NewSession(info DeviceInfo) (*Session, error)  // 绑定 UDP 39003
func (s *Session) Run(ctx context.Context) error
func (s *Session) Reset()
func (s *Session) handleScan(addr *net.UDPAddr)
func (s *Session) handleAuth(addr *net.UDPAddr)
func (s *Session) handleVersion(addr *net.UDPAddr)
func (s *Session) handleFile(addr *net.UDPAddr, data []byte)
func (s *Session) handlePacket(addr *net.UDPAddr, data []byte)
func (s *Session) validateAndReply(addr *net.UDPAddr)
func (s *Session) send(addr *net.UDPAddr, data []byte) error
```

`handlePacket` 伪代码：

```text
crc, num, payload, err := parsePacket(data)
if err != nil: return
if len(payload) != 分包长度字段: 回 packet_fail(原因=length)
if crc32.ChecksumIEEE(payload) != crc: 回 packet_fail(原因=crc)
assembler.Put(num, payload)
回 packet_ok(ID, crc, num, len(payload))
if assembler.Complete(): validateAndReply()
```

`validateAndReply`：

```text
if assembler.Validate() != nil: 回 valid_fail(原因)
if 解压失败: 回 valid_fail("zip")
回 valid_ok
异步进入 install（校验失败则不 install）
```

`handleVersion` 伪代码：

```text
target := parseVersionCommand(data)
if target 格式非法: 回 version_fail(ID, "format")
else: 回 version_ok(ID, 自己 cpdc_config.json 根节点 version)
```

### 6.6 install.go（T16）

```go
func Install(zipBytes []byte, info DeviceInfo) error
func unzipToTemp(zipBytes []byte) (string, error)
func findTypeDir(root, deviceType string) (string, error)
func overwriteJSON(typeDir string, info DeviceInfo) error
func overwriteInstallFile(typeDir, runDir, deviceType string) error
func overwriteINI(typeDir string, info DeviceInfo) error
func writeReceiptMarker(newVersion string) error
func restartSelf(cmd []string) error
```

- `findTypeDir`：找与设备类型同名的文件夹；找不到则停止。
- `overwriteJSON`：处理 `--config-path`（文件夹→默认 `cpdc_config.json`；文件→用该文件名）。
- `overwriteInstallFile`：按映射表取安装文件名，覆盖到运行目录。
- `overwriteINI`：仅当 `ReReadJson.ini` 存在才覆盖；`ReReadjsonPath` 有值→该目录，无值→运行目录。
- `writeReceiptMarker`：写运行目录 `.cpdc_update_receipt.json`（记录新版本）。
- `restartSelf`：强杀 + 用保存的运行命令重启。

### 6.7 receipt.go（T17）

```go
func readReceiptMarker() (string, bool)
func sendUpdateReceipt(addr *net.UDPAddr, d DeviceInfo, newVersion string, err error)
```

- 启动早期读 marker；存在则回 `update_ok`/`update_fail`；成功后删除 marker。

## 七、关键一致点（终版）

- CRC32：`crc32.ChecksumIEEE`。
- 字节序：全大端。
- ID：`[类型长度 uint16BE][类型][IP长度 uint16BE][IP]`。
- `packet:` 头 19 字节，分包载荷 ≤ 1381，整包 ≤ 1400，包号从 1，下标 = 包号-1。
- `file:` 的 CRC32 = 整 ZIP；`packet:` 的 CRC32 = 单个分包。
- 成功分包回 `packet_ok:`，失败回 `packet_fail:`（原因 `length`/`crc`）。
- `version:` 携带目标版本；CPDC 校验格式，合法回 `version_ok:`+当前版本，非法回 `version_fail:`（原因 `format`）。
- 「当前版本 == 目标版本」的比较在 **CPDS** 完成（相等→已最新，不等→需更新）；CPDC 不做该比较。
- `Scan` = 全局复位（终止在途任务 + 清 assembler + 复位状态）。
- 整体校验失败（`valid_fail`）→ 不写入、不重启、不回 update。
