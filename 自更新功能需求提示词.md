# 自更新功能需求提示词（最终版）

你是资深 Flutter 架构师 + 嵌入式设备远程升级专家。请先研读两个项目，再实现「自更新」。

## 背景
- CPDS：本项目（flutter_kts_template，Flutter 3.38.10）。已有 uploads、zipCache、注钥枪列表等目录/功能、ObjectBox 数据库、左侧菜单与页面。
- CPDC：运行在每个设备上的 Go 客户端，通过 UDP 广播接收 CPDS 下发的 ZIP 包。
- 目标：CPDS 把新的 CPDC 安装包下发并安装到各设备（远程自更新）。

## 通信模型
- 新增独立协议，与现有 cpd.proto / 现有下发功能完全区分开。
- UDP 广播；发每一条指令后必须启动一个超时器，等 CPDC 回复后清除超时器，再发下一条（含传输分包）。
- 例外：发现阶段 = 10 秒内每秒发一次，收集回复。
- 步骤弹窗中，任意取消/失败/意外终止/完成，都要关 UDP 并复位所有变量、缓存、状态信息。
- 除「发现 Scan」外，所有发送/等待都带重试：初始 1 次 + 重试 2 次 = 最多 3 次，连续 3 次超时/失败即终止。
- 所有 fail 回复的「原因」在线传英文码（`format`/`length`/`crc`/`size`/`zip`/`install_error`/`restart_error`/`version_read_error`/`version_mismatch`），UI 用 i18n 转中/英/阿拉伯语显示。
- 上行回复统一携带二进制设备标识 ID（见下）；不再使用文本 `#` 分隔。
- UDP 端口：CPDC 监听 39003，CPDS(flutter_kts_template) 监听 39004。

### 设备标识 ID

```text
ID = [类型字节长度 uint16BE] + [类型 UTF-8] + [IP字节长度 uint16BE] + [IP UTF-8]
```

所有上行回复（除 `Scan:success`）都携带 ID。

## 协议定义（字节级）

### 发现 Scan
- 发送指令前，复位所有变量/缓存。
- CPDS：每秒发 "Scan"，共 10 秒；结束收集结果，空则提示「未扫描到设备」；结束后再收到的 "Scan:success" 直接丢弃。
- CPDC：收到 "Scan" → 终止正在进行的任务、复位所有变量/缓存/状态（含清空 assembler 等自更新状态），并回复 "Scan:success"。

### 认证 Auth
- CPDS：发送 "auth"（小写），5 秒超时，表格 loading；超时重发（新超时器），连续 3 次 → 终止+关 UDP+复位+关 loading。收到至少一条回复 → 取消超时、关 loading、插入行；IP 重复显示 "IP（重复）"；随后立即进入版本校验。
- CPDC：回复 `"authAck:"` + ID（类型/IP 取自本地 `cpdc_config.<类型>.json` 的 `<类型>` 占位符与本地 IP）。

### 版本校验 Version
- CPDS：发送 `"version:"` + 目标版本号（用户上传时填写的 version），【当前版本】列三点跳动 loading，5 秒超时，超时重发，连续 3 次 → 终止。收到回复 → 关 loading，按「类型+原始IP」匹配行，把当前版本填入【当前版本】列。
- 新版本列直接 = 目标版本号（不再读文件夹内 cpdc_config.json 计算）。
- 版本比较在 **CPDS** 完成：当前版本 == 目标版本 → 「已最新」；不等 → 「需更新」；`version_fail` → 「无法获取/版本号格式非法」。此阶段结束暂停，不自动进入传输。
- CPDC：收到 `version:` 后校验后面版本号格式；合法 → 回 `"version_ok:"` + ID + 自己 cpdc_config.json 根节点 version；非法 → 回 `"version_fail:"` + ID + `format`。类型/IP 复用认证阶段值（避免多网卡导致 IP 不一致）。

### 传输 Transfer
- CPDS：勾选行 → 点【传输】；按勾选设备类型**去重**后挑选对应文件夹，打包成**一个** ZIP；整包 ≤ 1400 字节拆包。
    - 前置指令：`"file:"`(5B) + uint32BE 整ZIP CRC32 + uint32BE 包数量 + uint32BE 文件总字节 + 文件名称(N字节)；3 秒超时重发，连续 3 次 → 终止；需等所有勾选设备都回 `fileAck`。
    - 分包指令：`"packet:"`(7B) + uint32BE 本次分包 CRC32 + uint32BE 包号(从1) + uint32BE 分包长度 + 分包数据；3 秒超时重发，连续 3 次 → 终止；需等所有勾选设备都回 `packet_ok`。
    - 收到 `packet_fail`（长度/CRC 校验失败）→ 同样按「最多 3 次」重发该分包。
- CPDC：
    - 前置回复：`"fileAck:"` + ID + uint32BE 整ZIP CRC32 + uint32BE 包数量 + uint32BE 文件总字节 + 文件名称。
    - 分包通过：`"packet_ok:"` + ID + uint32BE 本次分包 CRC32 + uint32BE 包号 + uint32BE 分包长度。
    - 分包失败：`"packet_fail:"` + ID + uint32BE 本次分包 CRC32 + uint32BE 包号 + uint32BE 分包长度 + 原因(`length`/`crc`)。
    - CPDC 组装下标 = 包号-1，覆盖式写入；收齐后拼接成整 ZIP。

### 校验 Valid（CPDC 主动触发，CPDS 等待）
- CPDC 收齐分包（包号 == 包数量）后组装 ZIP，校验：字节数==文件总字节、CRC32 一致、可正常解压。
- 通过 → 回 `"valid_ok:"` + ID；失败 → 回 `"valid_fail:"` + ID + 原因（`size`/`crc`/`zip`）。
- CPDS：收到最后一包 `packet_ok` 后进入校验等待，启动 **5 秒**超时器，逐台收集 valid_ok/valid_fail；5 秒到仍有设备未回 → 标记「校验超时」；窗口结束后再收到 valid 回复直接丢弃。valid_ok → 进入写入；valid_fail → 标记校验失败；失败/超时设备不再等待 update 回执。

### 写入 Write
- CPDC 校验通过后写 ZIP → 解压：
    1. 按认证得到的设备类型匹配 安装文件/JSON/INI。
    2. 查询本设备 CPDC 运行命令（运行时保存参数）。
    3. 若运行命令含 --config-path：值为文件夹 → 默认取 cpdc_config.json；值为文件且非 cpdc-config.json → 用该文件名；将匹配到的 JSON 覆盖到该目录。
    4. 匹配到的安装文件覆盖到 CPDC 运行目录。
    5. 读 JSON 根节点 ReReadjsonPath：仅当匹配的 ini 文件存在时才覆盖；ReReadjsonPath 有值 → 覆盖 ini 到该目录，无值 → 覆盖 ini 到 CPDC 运行目录。
    6. 覆盖完成后，持久化「待回执」标记（记录新版本）。
    7. 强制终止 CPDC，再按运行命令重新运行。
- 匹配不到对应 JSON/INI 时无需覆盖。

### 回执 UpdateResult（重启后主动广播）
- CPDC 重启完成后，检测「待回执」标记 → 广播：
    - 成功：`"update_ok:"` + ID + 新版本
    - 失败：`"update_fail:"` + ID + 原因（`install_error`/`restart_error`/`version_read_error`/`version_mismatch`）
- 回执发送成功后清除标记，避免每次启动重复回执。
- CPDS：写入完成后进入回执，等待窗口 30 秒持续监听；按 ID 匹配行，标记 成功(vX.Y.Z)/失败(原因)/回执超时；只有校验通过设备才等待 update 回执。

## 功能点

### F1：cpdc_config.json 增加 version 字段
- 设备端 cpdc_config.json 根节点新增 version，值形如 "0.1.1.1"（点分四段）。
- 上传时 CPDS 把用户填写的 version 回写到解压目录「二级目录」下每个 generic `cpdc_config.json` 根节点（**不改** `cpdc_config.<类型>.json`）。
- CPDC 版本校验阶段读自己 cpdc_config.json 根节点 version 回传；版本比较在 CPDS 完成。

### F2：CPDS 新增上传页面
- 页面 = 表格 + 【文件上传】按钮（表格右上方）。
- 表格列：版本号、文件名称、备注、创建时间。
- 每行 3 个操作：编辑 / 删除 / 更新到设备（编辑仅改 version 与备注，保留文件名称与创建时间）。
- 左侧菜单新增入口；支持分页、搜索；默认创建时间降序。

### F3：文件上传流程
1. 必须 .zip（无大小上限），命名 install_yyyyMMdd_HHmmss.zip。
2. 保存到「注钥枪列表」保存路径同级的 install 文件夹。
3. 上传弹窗三个区域：
    - version：必填，仅数字，格式 0.1.1.1（四段）。
    - 备注：textarea，选填，最长 150 字。
    - 文件上传：只读输入框 + 【上传】按钮；上传中下方进度条，成功后消失，只读框显示文件名。
4. 上传成功后解压 ZIP：
    - 多个文件夹 → 新建 install_yyyyMMdd_HHmmss 文件夹，把解压内容移入。
    - 仅 1 个文件夹 → 重命名为 install_yyyyMMdd_HHmmss。
5. 存库：version、文件名称、备注、创建时间；相同 version 直接覆盖。

### F4：删除行
删除该行记录 + 删除 ZIP + 删除同名文件夹 install_yyyyMMdd_HHmmss。

### F5：更新到各设备
点击某行【更新到设备】：
1. 检查 UDP：未关闭则关闭，复位所有变量/缓存/状态。
2. 弹出「扫描倒计时」圆形进度框：三行居中文字「扫描中....」「已扫描到 XXX 个设备」「剩余秒」；底部【取消】。取消 → 复位+关 UDP。
3. 10 秒结束：空 → 提示「未扫描到设备」并中断+关 UDP+复位；非空 → 打开【步骤条+设备明细表】弹窗。

步骤条 7 步（顺序）：发现 → 认证 → 版本校验 → 传输 → 校验 → 写入 → 回执。
- 发现：倒计时框完成，进入时显示「已发现」。
- 认证：弹窗打开后立即执行。
- 版本校验：认证后立即执行，结束暂停。
- 传输：用户勾选行 + 点击【传输】执行。
- 校验：CPDC 主动 valid_ok / valid_fail。
- 写入：CPDC 落盘+重启。
- 回执：CPDC 重启后广播 update_ok / update_fail。

暂停/取消：
- 暂停：暂停向未完成设备发包（当前块发完即停），恢复断点续传。
- 取消：停止发送，未完成设备标记「失败/已取消」；已完成设备保持已更新，不回滚。

## 设备明细表
列：IP 地址 / 类型 / 当前版本 / 新版本 / 状态 / 进度 / 结果。
- IP 重复时显示 "IP（重复）"。
- 认证成功设备置顶；表格可滚动、表头固定。
- 各步骤列显示：
  · 认证：状态=认证中/成功/失败
  · 版本校验：当前版本、新版本填充，状态=需更新/已最新/无法获取
  · 传输：状态=传输中/完成/失败，进度=百分比+已传/总大小
  · 校验：状态=校验中/通过/失败
  · 写入：状态=安装中/成功/失败
  · 回执：结果=成功(vX.Y.Z)/失败(原因)/回执超时

按钮状态机（Idle/Running/Paused/Finished）：
| 按钮 | Idle | Running | Paused | Finished |
| 开始 | 可用 | 禁用 | 可用(继续) | 禁用 |
| 暂停 | 禁用 | 可用 | 禁用 | 禁用 |
| 取消 | 可用(关闭) | 可用 | 可用 | 禁用 |
| 关闭 | 可用 | 禁用 | 可用 | 可用 |

## 安装包 ZIP 目录结构（按设备类型命名，每类文件夹可能不存在）
- 目录名 = 设备类型（与 CPDC 回复的 `<类型>` 同名）。

| 设备类型(目录名) | 安装文件 |
|---|---|
| MR9360 | CPDC-HF |
| MMR200 | CPDC-MultiBandRadio |
| PMR200 | CPDC-MultibandHandheld |
| CCU | CPDC-CCU |
| CCUAudio | CPDC-CCU-Audio |
| VehInter | CPDC-VehInte |

- 每类文件夹内：安装文件(必存在)、cpdc_config.json(不一定存在)、cpdc_config.<类型>.json(不一定存在)、ReReadJson.ini(不一定存在)。
- `Server`/`IEC`/`SmallHandheld` 无对应更新文件夹：认证阶段收到这三类类型时 CPDS 直接跳过、不加入设备表（其中 Server 与 CCU 共用 UDP/JSON，不会上报）。
- `VehInter`（类型/文件夹）与 `CPDC-VehInte`（安装文件）拼写不同，按表精确匹配。

## 输出要求
- 实现前列出「受影响文件/模块」清单。
- 外科手术式修改；UDP 等硬件逻辑测试必须 mock。
